# 真机调试计划 · 2026-05-28

> 结合 2026-05-27 夜间产出（`goal_nav` / `seven_point_test` / `ten_point_race` / `ten_point_odom_race` 四个脚本 + localize 流程，**均未真机验证**）。
> 从安全到风险、单步推进。边跑边勾。

## 铁律（每一步都遵守）

- [ ] 运动由人跑；**急停在手**，脚本贴挡板立刻拍。
- [ ] **单步跑、停下量、绝不打包连跑**（上次连跑累计漂 3.4m 的教训）。
- [ ] 车先摆**比赛原点、车头朝 +x**，再起 `bringup`（odom 在此归零）。
- [ ] 新脚本**不在 `abot` 包内** → 用 `python2 <绝对路径>` 跑，别用 `rosrun abot`。

---

## Stage 0 · 上车 + 摆位（无运动）

- [ ] 车上 repo 目录 `git pull`
- [ ] `ls src/abot_project/scripts/*.py` 见到 goal_nav / seven_point_test / ten_point_race / ten_point_odom_race
- [ ] `ls src/abot_project/abot/maps/` 有 `comp.yaml`（launch 实际加载这份）
- [ ] **把车上场地图备份进 git**（命根子，别只躺车上）：
      ```bash
      # 车上 ~/catkin_ws 执行
      git add src/abot_project/abot/maps/comp.pgm src/abot_project/abot/maps/comp.yaml
      git commit -m "backup: 比赛场地图 comp.*"
      git push
      ```
      若有现场照片也一并放 `src/abot_project/abot/maps/` 提交。
- [ ] 车摆比赛原点、车头朝 +x

## Stage 1 · 底盘层（无/微动）

- [ ] `roslaunch abot bringup.launch`（env 空报错就用 `bash -ic`）
- [ ] `rostopic echo -n1 /odom` 位置 ≈ 0
- [ ] `/imu/data` 有数据
- [ ] `rosrun tf tf_echo odom base_footprint` 有输出
- 失败：odom 非 0 → 重启 bringup 归零

## Stage 2 · 定位层（无运动｜验证「清残留」修复）

- [ ] 先跑速查 §0「清残留进程」（killall + pkill）
- [ ] `roslaunch abot localize.launch`
- [ ] `rostopic hz /scan` 有频率
- [ ] `rosrun tf tf_echo map base_footprint` 有输出（amcl 收敛）
- [ ] rviz 粒子云收拢
- 失败：`No laser scan` / `map does not exist` → 残留进程没清干净，重跑 §0

## Stage 3 · goal_nav 单个近目标（首次受控运动，最安全）

- [ ] `python2 ~/catkin_ws/src/abot_project/scripts/goal_nav.py`
- [ ] rviz 加 Path display，topic `/goal_nav/path`
- [ ] 点一个 **0.5m 内的近目标**
- [ ] 先看到蓝色规划线（轴对齐、绕障）**再**动
- [ ] 车直行 + 原地转到点，不斜穿、不暴走
- 失败：冲过头 → `V_LIN`/`K_LIN` 偏大；规划线穿墙 → `~inflation_radius` 0.20→0.30

## Stage 4 · seven_point_test（7 点纯导航，量精度）

- [ ] `python2 .../seven_point_test.py`（前置 bringup + localize）
- [ ] 7 点全部 "reached"，无 TIMEOUT
- [ ] **卷尺量终点误差 < 4cm**
- [ ] 全程不碰挡板
- 失败：某点 TIMEOUT → 该点在膨胀/墙边；偏差大 → amcl 漂或 `linear_scale`

## Stage 5 · ten_point_odom_race（保底，仅 bringup，看漂移）

- [ ] 只起 `bringup` → `python2 .../ten_point_odom_race.py`
- [ ] 跑完 10 点
- [ ] **卷尺量末点 vs 真实原点的累计漂移**（决定保底可用性）
- 备注：纯里程计无校正，漂移是预期；这是保底不是首选

## Stage 6 · ten_point_race（完整比赛：路线 + 抓放）

- [ ] 前置：bringup + localize **+** `vl_locate.launch`(conda 39) + `grasp.launch`
- [ ] **赛前把 WAYPOINTS 里 P1/P3/P5 的 object 改成裁判公布的关键词**
- [ ] `python2 .../ten_point_race.py`
- [ ] 10 点路线走对（去抓取点前先过放置点过渡）
- [ ] 抓放握手成功（`/grab`、`/vlm_detection`）
- [ ] 不撞挡板、无 30s 卡死
- 失败：抓放卡住 → 查 `/vlm_detection`/`/grab`；导航问题回 Stage 4

## Stage 7 · 收尾（无运动，清债）

- [ ] **统一 `auto_task_runner.py`**：确认队友 `72dd6d9` 车上版为准 → 同步回 `scripts/`、删 `bags/` 脏副本
- [ ] **cartographer `rplidar_lds.lua`**：仅当要重新建图才 scp 上车（修双发冲突）；`comp.yaml` 已有就跳过
- [ ] **打一个车上实况快照分支** `car-snapshot/2026-05-28`（场地图/params/车上脚本现状），见 [`CONTRIBUTING.md`](../CONTRIBUTING.md) §7

> 🔔 **给队员**：每搞一段时间 / 每次大调整后，就按 CONTRIBUTING §7 把车上实况打个 `car-snapshot/日期` 分支留底——别等 SD 卡挂了才后悔。

---

## 优先级

**命门 = Stage 2（定位稳）+ Stage 4（精度达标）**——这两步过了比赛才能跑。
Stage 6 决定拿分上限；Stage 5 保底；Stage 3 是最稳的第一脚。
