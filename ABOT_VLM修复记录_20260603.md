# ABOT VLM 检测修复记录 — 2026-06-03

## 概览

| 项目 | 值 |
|------|-----|
| 会话日期 | 2026-06-03 |
| 小车状态 | 上次会话 OOM 死机，断电重启后恢复 |
| 修改文件 | `server.py`（3处改动） |
| 小车 IP | 192.168.43.211 |
| 环境 | ROS Melodic, Python 3.9 (conda env 39), openai 兼容模式 |

---

## 改动 1：server.py — 添加颜色映射（英文→中文）

**位置**：`server.py` 第 41 行后（`__init__` 中，`self.text_color` 之后）

**原因**：Qwen VL 模型在中文 system prompt 上下文中识别英文颜色名（`red`/`blue`）效果差，返回空列表 `[]`。使用中文颜色名（`红色`/`蓝色`）可正常检测。

**新增代码**：
```python
# 颜色映射：英文→中文（Qwen VL 在中文上下文下识别英文颜色名效果差）
self.color_map = {
    'red': '红色', 'blue': '蓝色', 'green': '绿色', 'yellow': '黄色',
    'orange': '橙色', 'purple': '紫色', 'black': '黑色', 'white': '白色',
    'pink': '粉色', 'brown': '棕色', 'gray': '灰色', 'grey': '灰色',
}
```

**回档方法**：删除 `self.color_map = {...}` 块即可。

---

## 改动 2：server.py — 异常处理扩展 + 空列表优雅降级

**位置**：`server.py` 第 134-144 行（`yi_vision_api` 方法的 `try/except` 块）

**原因**：
- 原代码只捕获 `json.JSONDecodeError`，API 返回空列表 `[]` 时触发 `IndexError`（`result[0]`），未被捕获导致服务报错崩溃
- 需同时处理空列表 `[]`（模型未检测到目标）和非 dict 返回值

**旧代码**：
```python
        try:
            result = json.loads(content)
            # 如果返回的是列表，取第一个元素
            if isinstance(result, list):
                result = result[0]
            rospy.loginfo('大模型调用成功！')
            return result
        except json.JSONDecodeError as e:
            rospy.logerr(f"JSON解析失败: {e}")
            rospy.logerr(f"返回内容: {content}")
            return None
```

**新代码**：
```python
        try:
            result = json.loads(content)
            # 如果返回的是列表，取第一个元素
            if isinstance(result, list):
                if len(result) == 0:
                    rospy.logwarn('大模型未检测到目标（返回空列表），请确认物体在视野内')
                    return None
                result = result[0]
            if not isinstance(result, dict):
                rospy.logerr(f'返回数据格式异常（非dict）: {type(result)}')
                return None
            rospy.loginfo('大模型调用成功！')
            return result
        except (json.JSONDecodeError, IndexError, KeyError, TypeError) as e:
            rospy.logerr(f"返回数据解析失败: {e}")
            rospy.logerr(f"返回内容: {content[:200]}")
            return None
```

**回档方法**：把 `try/except` 块和 `if isinstance(result, list)` 块替换为旧代码。

---

## 改动 3：server.py — vlm_detection 使用中文颜色名

**位置**：`server.py` 第 176 行（`vlm_detection` 方法）

**原因**：配合改动 1，将用户传入的英文颜色名转换为中文后再拼入 prompt。

**旧代码**：
```python
    def vlm_detection(self):
        result = self.yi_vision_api('检测图中的{}'.format(self.object_name))
```

**新代码**：
```python
    def vlm_detection(self):
        # 颜色名转中文（Qwen VL 在中文上下文下识别英文颜色名效果差）
        obj_name_cn = self.color_map.get(self.object_name, self.object_name)
        result = self.yi_vision_api('检测图中的{}'.format(obj_name_cn))
```

**回档方法**：删除 `obj_name_cn = ...` 行，改回 `'检测图中的{}'.format(self.object_name)`。

---

## 未改动但已识别的问题

### 问题 A：resize.py 裁剪区域过于激进

**位置**：`vl_locate/scripts/resize.py` 第 43-46 行

**现状**：
```python
x1 = int(width * 0.43)    # 只保留画面 43%-75% x
y1 = int(height * 0.53)   # 只保留画面 53%-78% y
x2 = int(width * 0.75)
y2 = int(height * 0.78)
```

**问题**：VLM 只能看到画面右下角一小块。红色物体在画面左侧（x=124~249，约 19%~39%）时完全被裁掉，API 返回空列表。

**验证数据**（2026-06-03 测试）：
- 全图输入 API → ✅ 检测到红色，bbox `[124, 250, 249, 316]`
- 裁剪图输入 API → ❌ 返回 `[]`

**建议**：改为全图或扩大裁剪范围，或将裁剪参数做成 rosparam 可配置。

### 问题 B：vl_locate.launch 硬编码 mode

**位置**：`vl_locate/launch/vl_locate.launch` 第 12 行

**现状（车上已修，git 未同步）**：
- 车上：`<param name="mode" value="openai" />` ✅
- git 仓库：`<param name="mode" value="dashscope" />` ❌

**说明**：launch 文件的 `<param>` 会覆盖 YAML 配置文件中的值。dashscope SDK 有 403 bug，openai 兼容模式可用。

### 问题 C：CAMERA 环境变量缺失导致 vl_locate 启动失败

**症状**：`roslaunch vl_locate vl_locate.launch` → `Invalid <arg> tag: environment variable 'CAMERA' is not set`

**修复**：启动前 `export CAMERA=gemini`

---

## 测试验证

| 测试 | 修复前 | 修复后 |
|------|--------|--------|
| `rosservice call /vlm_detection "red"` | ❌ `IndexError: list index out of range` | ✅ `success: False, "检测失败"` |
| `rosservice call /vlm_detection "blue"` | ❌ 同上崩溃 | ✅ `success: True, (344, 145)` |

---

## 文件位置

| 文件 | 本地 git 路径 | 车上路径 | 同步状态 |
|------|-------------|---------|---------|
| server.py | `E:\abot_study\small_car\src\abot_project\vl_locate\scripts\server.py` | `/home/abot/catkin_ws/src/abot_project/vl_locate/scripts/server.py` | ✅ 已同步 |
| vl_locate.launch | `E:\abot_study\small_car\src\abot_project\vl_locate\launch\vl_locate.launch` | (车上第12行已改为 openai) | ⚠️ git 未同步 |
| resize.py | `E:\abot_study\small_car\src\abot_project\vl_locate\scripts\resize.py` | 未改动 | — |

---

## 启动命令速查（备忘）

```bash
# 环境变量（SSH 不自动加载 .bashrc）
export ROS_MASTER_URI=http://192.168.43.211:11311
export ROS_IP=192.168.43.211
export ABOTMODEL=x4 ABOTBASE=omni ABOTIMU=mpu6050 ABOTLIDAR=rplidar CMM_MODE=rosserial CAMERA=gemini
source /opt/ros/melodic/setup.bash
source ~/catkin_ws/devel/setup.bash
export PATH=/home/abot/anaconda3/envs/39/bin:$PATH

# 启动顺序
roscore &
roslaunch abot bringup.launch &           # ① 底盘+传感器
roslaunch vl_locate vl_locate.launch &    # ② VLM 检测（含 camera）
roslaunch ZachLab_grasp grasp.launch &    # ③ 机械臂
rosparam set /grab 1                      # ④ 使能抓取

# 测试检测
rosservice call /vlm_detection "object_name: 'red'"
rosservice call /vlm_detection "object_name: 'blue'"
```
