# 小车启动命令速查

## 启动顺序（5个终端）

### 终端1: 底盘 + 传感器
```bash
roslaunch abot bringup.launch
```

### 终端2: 定位 + DWA 导航
```bash
roslaunch abot navigate.launch
```

### 终端3: VLM 视觉检测链路
```bash
conda activate 39
roslaunch vl_locate vl_locate.launch
```

### 终端4: 机械臂
```bash
roslaunch ZachLab_grasp grasp.launch
```

### 终端5: 运行10点导航抓取脚本
```bash
python -u /home/abot/catkin_ws/src/abot_project/scripts/auto_navigation_grasp.py
```

## 常用工具命令

### 键盘遥控（跑点位用）
```bash
rosrun teleop_twist_keyboard teleop_twist_keyboard.py
```

### 查看当前位姿（map→base_footprint）
```bash
rosrun tf tf_echo map base_footprint
```

### 舵机回初始位置
```bash
rostopic pub -1 /servo riki_msgs/Servo "{Servo1: 88, Servo2: 20}"
```

### 发送单个导航目标点
```bash
rostopic pub -1 /move_base_simple/goal geometry_msgs/PoseStamped '{header: {frame_id: "map"}, pose: {position: {x: 0.0, y: 0.0, z: 0}, orientation: {x: 0, y: 0, z: 0, w: 1}}}'
```

### 查看摄像头画面
```bash
rqt_image_view /camera/rgb/image_raw
```

### 激光扫描频率
```bash
rostopic hz /scan
```

### 图像帧率
```bash
rostopic hz /camera/rgb/image_raw
```
