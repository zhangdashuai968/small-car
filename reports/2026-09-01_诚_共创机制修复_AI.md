# 会话报告（AI 版）— 共创机制修复

> 此报告由 AI（ZCode/GLM）生成，人类队友确认后提交
> 文件名：`2026-09-01_诚_共创机制修复_AI.md`

## 元数据（机器解析用）

```yaml
session:
  date: "2026-09-01"
  executor: "诚"
  experiment: "NONE"
  duration_minutes: 240
  success: true

git:
  commit_before: "50827f5"
  commit_after: "61f86b1"        # 代码/文档落库终点；本报告与日志随收尾提交再上一个
  files_changed:
    - path: "claude.md"
      change_type: "deleted"
      description: "移除大小写重复跟踪（NTFS 单物理文件，防 add -A 双写互覆）"
    - path: "src/abot_project/vl_locate/scripts/server.py"
      change_type: "modified"
      description: "移除错误的 unzoom 反算（标定在 zoomed 图像空间），保留 df7a387 健壮性增强"
    - path: "scripts/pipeline_sim.py"
      change_type: "added"
      description: "VLM 抓取全链路数学仿真，验证无需 unzoom"
    - path: "CLAUDE.md / CONTRIBUTING.md / reports/模板_人工报告.md / scripts/auto-sync.sh / sync-once.*"
      change_type: "modified"
      description: "机制改造：auto-sync 弃用→AI 会话收尾制、AI 直连真机、报告双轨减负、车上快照改 SSH 拉取、防大小写复雷"
    - path: "README.md / docs/* / scripts/waypoints.yaml"
      change_type: "modified"
      description: "航线正本口径、qwen3.7-plus 对齐、TEB 未入库参数标注、WORKFLOW 死链修复、汇总 HTML 重生成"

vehicle_state:
  bringup_ok: null        # 本次未启动底盘
  navigation_ok: null
  slam_ok: null
  arm_ok: null
  battery_voltage: 0.0    # 未测
```

## 执行的操作序列

```bash
# 1. 真机连通（Windows → 车免密 SSH，ZCode 远程表单因 GLIBC 2.28 死路）
ssh abot@192.168.43.211        # 车本次在手机热点网段，DHCP 认 MAC 稳定
# 2. 三方哈希对比，查明 server.py 三分：车=6/11 前旧版 / GitHub=含错误 unzoom / 本地=修正版未提交
# 3. 拆大小写雷 + 分组落库（本地 4 commits: 6c24079/f6c361b/5f79442/61f86b1）
git push origin main            # 50827f5..61f86b1，SSH 直连成功
# 4. 部署 server.py 修正版上车
#    车: cp server.py server.py.bak-20260901-pre-fix → scp → sed CRLF → py_compile OK → md5=04cf22ad（=本地=GitHub blob 026ebb9）
# 5. 清失效设施：schtasks /delete \small-car-auto-sync；三个同步脚本头部加弃用标注
# 6. python tools/gen_docs.py   # 汇总 HTML 重生成（77913B）
```

## 参数变更

| 文件 | 参数名 | 旧值 | 新值 | 原因 |
|------|--------|------|------|------|
| server.py | unzoom_coordinates() | 存在（GitHub 版） | 移除 | 标定在 zoomed 空间，反算反而错；真机旧版行为已验证 |

## 异常记录

### 异常 1
- **症状**：ZCode 远程表单连接失败 `GLIBC_2.28 not found`
- **根因**：ZCode 自带 node 需 GLIBC≥2.28，车是 18.04 只有 2.27
- **修复**：弃用表单，改 Windows 免密 SSH 直控（密钥认证）

### 异常 2
- **症状**：`git commit -- claude.md` 提交形态错误（内容更新 55+/22-，非删除）
- **根因**：pathspec 提交读取工作区状态，NTFS 大小写不敏感把物理 CLAUDE.md 内容写回 claude.md 路径
- **修复**：`git reset --soft HEAD~1` → `git restore --staged CLAUDE.md` → 纯暂存删除提交（-126 行）

### 异常 3（发现未处理）
- **症状**：`server.py` 源码硬编码 qwen API key 兜底值（`~api_key` param 的 default），仓库为 public
- **根因**：历史遗留；CLAUDE.md 安全条款（密码仅 ssh-car.py 可硬编码）未被代码遵守
- **建议**：轮换该 key；default 改为空并在 launch/本地参数文件注入

## 传感器/硬件状态

| 设备 | 状态 | 备注 |
|------|------|------|
| SSH/网络 | OK | 43.211 热网段；禁 ping，找车用 22 端口扫 |
| docker | OK | 空装；已配 daocloud+1ms.run 加速源（alpine 实测通过） |
| VLM 服务 | 未启动 | server.py 已部署修正版，重启 vl_locate 才生效 |

## 未解决的问题

- [ ] bags/auto_task_runner.py 孤儿副本处置（车上无 ~/bags，等诚拍板删/标注）
- [ ] server.py 内硬编码 qwen key 轮换
- [ ] CMM_MODE 环境变量是否仍必需（下次上机实测）
- [ ] TEB 两个参数（weight_max_vel_theta/oscillation_timeout）是否入库（TROUBLESHOOTING 已标注未入库）
- [ ] 智谱化四项（GLM 上车/AGENTS.md/model 署名/收尾命令化）等诚拍板

## 下一次会话建议

1. 真机验证：重启 vl_locate 后跑一次抓取对拍（修正版 vs 备份版），确认后删车上 `.bak`
2. 先处理 qwen key 轮换（public 仓库暴露中）
3. 会话启动按 CLAUDE.md 新清单：git pull + 核对中枢文档日期

---

> 关联：人工速记待填 `2026-09-01_诚_共创机制修复.md`（5 行减负版）
