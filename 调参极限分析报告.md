# 差速轮小车 L 形路径调参极限分析报告

> 日期：2026-05-26
> 实验范围：Run 4 ~ Run 10（共 7 次有效实验）
> 结论：**调参已到结构极限，继续调参无法解决问题，必须变更控制架构。**

---

## 1. 问题定义

小车执行 body 帧 L 形曼哈顿分解路径（先前进 38cm，再左移 280cm，最后原地旋转到目标朝向）时，出现三类异常：

| 异常 | 表现 | 期望 |
|------|------|------|
| X 方向漂移（后退） | 左移阶段车身向后方滑动 6~16cm | 0cm（纯侧移不应有前后分量） |
| Y 方向超调 | 左移距离超出目标 6~11cm | ≤4cm（2% 以内） |
| 车身 wobble | 左移过程中车身扭动，轨迹非直线 | 直线平移 |

---

## 2. 根因分析

### 2.1 物理约束

小车底盘是**差速轮**（ABOTMODEL=x4），但系统配置为 `ABOTBASE=omni`（全向轮模式）。

差速轮只有两个原生自由度：**前进（tw.linear.x）** 和 **原地旋转（tw.angular.z）**。它**不具备**全向轮的侧移能力（tw.linear.y）。

### 2.2 指令层面的矛盾

当前控制代码 `_drive_to_point()`（`auto_task_runner.py:125`）在 map 帧计算出 `(vx_map, vy_map)` 后，通过标准旋转矩阵转换为 body 帧：

```python
tw.linear.x =  c * vx_map + s * vy_map   # c=cos(yaw), s=sin(yaw)
tw.linear.y = -s * vx_map + c * vy_map   # ← 这条指令差速轮无法执行
```

在 L 形路径的左移阶段，`vy_map` 是主运动分量（目标 0.2m/s），`vx_map` 是 P 控制器输出的修正量（用于抵消漂移）。

### 2.3 泄漏速度的数学推导

设车身当前朝向为 `θ`（即 hold_yaw），左移阶段 vy_map 为主分量。则 body 帧下：

```
tx_body_X = cos(θ) * vx_map + sin(θ) * vy_map
```

当 θ = 68°（Run 7b/9/10 的典型朝向）时：

```
泄漏速度 = sin(68°) × 0.2 ≈ 0.185 m/s
```

这意味着：**控制器还没有发出任何 vx_map 修正量，body X 方向就已经有 0.185 m/s 的寄生速度**。

### 2.4 P 控制器饱和证明

P 控制器的 vx_map 输出受 `V_LIN = 0.2 m/s` 硬钳制（`auto_task_runner.py:143`）：

```python
vx_map = _clamp(K_LIN * ex, -V_LIN, V_LIN)
```

而 vx_map 要抵消的泄漏速度是 `sin(θ) * vy_map`。稳态条件为：

```
K_LIN × ex = -sin(θ) × vy_map
ex = -sin(θ) × vy_map / K_LIN
```

代入 θ=68°, vy_map=0.2, K_LIN=4.0：

```
ex = -0.927 × 0.2 / 4.0 = -0.046m = -4.6cm
```

这个 `ex = -4.6cm` 已经是稳态误差——在理想条件下，小车**必须**偏离目标至少 4.6cm 才能让 P 控制器产生足够的对抗力。

但实际问题是：MCU 收到 `tw.linear.y` 后根本无法执行纯侧移，会将 omni 指令混算为等效的"侧前方"运动。这导致：

1. **vy_map 的实际执行效果不可预测**——取决于 MCU 的混算策略
2. **vx_map 的修正效果被 MCU 混算扭曲**——它也在混算路径上
3. **P 控制器在饱和边界工作**——K_LIN 增大到 4.0 后 vx_map 已触碰 ±V_LIN 钳制，进一步增大增益无效

---

## 3. 实验数据：调参无改善的实证

### 3.1 终点精度汇总（map 帧）

| Run | K_LIN | ΔX | ΔY | 距离误差 | 角度误差 |
|-----|-------|-----|-----|----------|----------|
| 6 | 1.0 | -15.5cm | +6.0cm | 16.6cm (5.8%) | -6.7° |
| 7 | 2.5 | -10.6cm | +6.5cm | 12.4cm (4.4%) | +3.4° |
| 7b | 2.5 | -12.0cm | -1.1cm | 12.1cm (4.3%) | +4.1° |
| 9 | 2.5 | -6.1cm | +4.2cm | 7.4cm (2.6%) | +1.2° |
| 10 | 4.0 | -13.1cm | -2.0cm | 13.2cm (4.7%) | +2.8° |

### 3.2 左移阶段 body 帧漂移分析

| Run | K_LIN | body X 漂移 | body Y 漂移 | 主问题 |
|-----|-------|------------|------------|--------|
| 6 | 1.0 | **-15.5cm** 后退 | +6.0cm 超调 | 严重后退 |
| 7 | 2.5 | **-7.9cm** 后退 | +9.6cm 超调 | 后退 |
| 7b | 2.5 | **-5.6cm** 后退 | +10.7cm 超调 | 后退 |
| 9 | 2.5 | +1.7cm 前进 | +7.3cm 超调 | 左超调 |
| 10 | 4.0 | **-6.8cm** 后退 | +11.4cm 超调 | 后退 |

### 3.3 三条不可动摇的规律

**规律 1：Y 方向超调恒定且不可改善**

所有 5 次实验的 Y 方向（左移）全部超调 +6 ~ +11cm。K_LIN 从 1.0 到 4.0，超调量**无任何改善趋势**——因为这取决于 MCU 对 `tw.linear.y` 的混算结果，P 控制器无法干预 MCU 内部逻辑。

**规律 2：X 方向后退有改善上限**

K_LIN 1.0 → 2.5 时，X 后退从 15.5cm 降至 ~7cm（改善约 50%）。但 K_LIN 2.5 → 4.0 时，后退反而回到 6.8cm——**改善已触底，进入噪声波动区间**。

这符合 2.4 节的理论预测：当 P 控制器输出触碰 ±V_LIN 钳制后，增大增益不再产生额外修正力。

**规律 3：好结果不可复现**

Run 9（X +1.7cm，距离 2.6%）是唯一一次接近理想的结果，但后续 Run 10 在相同条件下无法复现——说明那次"好"是 AMCL 位姿估计的偶然波动，不是控制策略在起作用。

---

## 4. 为什么继续调参无效

### 4.1 已尝试的策略

| 策略 | 结果 |
|------|------|
| K_LIN 从 1.0 增至 2.5 | X 漂移改善约 50%，Y 无改善 |
| K_LIN 从 2.5 增至 4.0 | **无进一步改善**（vx_map 已饱和） |
| K_LIN_HOLD = 5.0（非主运动轴高增益） | **无效**（同样受 V_LIN 钳制） |
| 多次重复实验（7b、9、10） | 误差在噪声范围内波动，无趋势 |

### 4.2 可调参数的剩余空间

当前可调参数已全部触碰边界：

- `V_LIN = 0.2`：已是最优平衡（再高→AMCL 位姿估计发散；再低→稳态误差更大）
- `K_LIN = 4.0`：输出已受 V_LIN 钳制，增大无意义
- `K_LIN_HOLD = 5.0`：同上
- `K_YAW = 1.5`：与 XY 漂移无关
- `POS_TOL / YAW_TOL`：不影响轨迹质量

**没有任何一个参数可以改变"MCU 无法执行 tw.linear.y"这个物理事实。**

---

## 5. 结论

**问题的本质不是参数不够好，而是控制架构与硬件不匹配。**

差速轮底盘被当作全向轮底盘发指令（`tw.linear.y`），MCU 收到无法执行的指令后自行混算，导致轨迹出现不可控、不可建模的漂移。P 控制器无论增益多大，都只能修正"指令→执行"这个闭环的外层误差，无法修正 MCU 内部的混算行为。

### 建议方案

废弃 `tw.linear.y`，只用差速轮原生自由度：

```
左移 = 旋转 90° 朝目标 → 直线前进 → 旋转回目标方向
```

这样每条路径段都只涉及 `tw.linear.x`（前进）+ `tw.angular.z`（旋转），两个指令差速轮 MCU 都能精确执行，轨迹将笔直、可复现。

---

*附录：全部实验原始数据见 `小车调节日志.txt`*
*关键代码：`scripts/auto_task_runner.py` → `_drive_to_point()` 方法*
