# ABOT M1 小车 TEB 参数修改记录

> 修改日期：2026-06-01
> 修改人：Claude（通过 SSH 远程修改）
> 小车 IP：192.168.43.211
> 用户：abot
> sudo 密码：123456

---

## 修改清单总览

| # | 文件（车载路径） | 变更性质 | 目的 |
|---|-----------------|---------|------|
| 1 | `.../teb/teb_omni_planner_params.yaml` | 参数调优 | 真机尺寸+后退偏好+算力优化 |
| 2 | `.../common/costmap_x4_params.yaml` | 🔴 关键修复 | 代价地图尺寸与实际一致 |
| 3 | `.../lidar/x4_laserfilter.yaml` | 微调 | 雷达自滤波覆盖完整机身 |
| 4 | `.../common/local_costmap_params.yaml` | 🔴 关键修复 | TF 延迟容限（解决撞墙） |
| 5 | `.../common/global_costmap_params.yaml` | 🔴 关键修复 | TF 延迟容限（解决撞墙） |
| 6 | `/etc/systemd/system/cpu-performance.service` | 新增文件 | CPU 锁频 1.48GHz 持久化 |

> 所有被修改文件均已生成 `.bak` 备份，位于原文件同目录。

---

## 1. TEB 规划器参数

**文件**：`/home/abot/catkin_ws/src/abot_project/abot/param/navigation/teb/teb_omni_planner_params.yaml`
**备份**：`teb_omni_planner_params.yaml.bak`

### 1.1 机器人尺寸（适配 90cm 通道）

| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `footprint_model.vertices` | 默认 | `[[-0.18,-0.15],[-0.18,0.15],[0.18,0.15],[0.18,-0.15]]` | 36cm长×30cm宽多边形 |
| `min_obstacle_dist` | 0.25 | **0.1** | 最小安全距离（通道90cm够用） |
| `inflation_dist` | 0.6 | **0.35** | 膨胀层距离 |

### 1.2 后退偏好（前进惩罚）—— 三轮迭代

> 用户需求："偏好后退，惩罚前进，weight=0.2，保留前进功能，只是倾向于后退"
> 最终形态：后退速度上限是前进的 2.7 倍，优化器代价函数中自然偏向后退方向。

#### 迭代过程

| 轮次 | 做了什么 | 问题 |
|------|---------|------|
| 第一轮 | `weight_kinematics_forward_drive: 0.2` | ❌ 对全向底盘不生效 |
| 第二轮 | `max_vel_x: 0.0` + `weight_max_vel_x: 10` | ❌ 前进完全锁死，用户反馈"太极端" |
| 第三轮 | `max_vel_x: 0.15` + `weight_max_vel_x: 3` | ✅ 当前方案 |

#### 为什么 `weight_kinematics_forward_drive` 不生效？

这是本次修改中最关键的技术发现。查阅了车载 TEB 源码：

**文件**：`/opt/ros/melodic/include/teb_local_planner/g2o_types/edge_kinematics.h`

```cpp
// 该约束边的 computeError() 内部：
// positive-drive-direction constraint
Eigen::Vector2d angle_vec(cos(conf1->theta()), sin(conf1->theta()));
_error[1] = penaltyBoundFromBelow(deltaS.dot(angle_vec), 0, 0);
// 注释明确标注："only diffdrive robot"
```

- `EdgeKinematicsDiffDrive`（差速底盘边）包含前进方向惩罚逻辑
- `EdgeKinematicsCarlike`（全向底盘边）不包含此惩罚
- ABOT M1 是麦克纳姆轮全向底盘，走的是全向运动学边
- 因此 `weight_kinematics_forward_drive` 参数被**静默忽略**

#### 等效替代方案：非对称速度边界

全向底盘的 X 方向速度约束位于：

**文件**：`/opt/ros/melodic/include/teb_local_planner/g2o_types/edge_velocity.h`（第 113 行）

```cpp
_error[0] = penaltyBoundToInterval(vel,
    -cfg_->robot.max_vel_x_backwards,   // 下界（后退）
     cfg_->robot.max_vel_x,             // 上界（前进）
     cfg_->optim.penalty_epsilon);       // 缓冲带
```

数学形式：

$$
e(v_x) = \begin{cases}
0 & v_x \in [-0.35,\; -0.1] \quad \text{后退自由区} \\[4pt]
v_x + 0.1 & v_x > -0.1 \quad \text{停止/前进，惩罚生效} \\[4pt]
-v_x - 0.35 & v_x < -0.35 \quad \text{后退超速，惩罚生效}
\end{cases}
$$

$$
f_{v_x} = \boxed{3} \cdot \sum e(v_x)^2
$$

- 后退自由区（无惩罚）：v_x ∈ [-0.35, -0.1] m/s，即机器人被驱动作 0.1~0.35 m/s 后退
- 停止 (v_x=0) 会产生残差 e = 0 + 0.1 = 0.1，代价 = 3 × 0.01 = 0.03
- 前进 (v_x=0.15) 会产生残差 e = 0.15 + 0.1 = 0.25，代价 = 3 × 0.0625 = 0.1875
- 优化器在自由区有 0.1~0.35 m/s 的零代价空间，自然偏好后退；前进也能走但有代价

#### 最终参数对照

| 参数 | 原始值 | 第二轮（过于激进） | 第三轮（当前） |
|------|--------|-------------------|---------------|
| `max_vel_x` | 0.4 | 0.0 | **0.15** |
| `max_vel_x_backwards` | 0.15 | 0.4 | **0.4** |
| `max_vel_y` | 0.4 | 0.25 | **0.25** |
| `weight_max_vel_x` | 2 | 10 | **3** |
| `weight_kinematics_forward_drive` | 0.5 | 0.2 | **0.2**（装饰性，不生效） |
| `allow_init_with_backwards_motion` | false | true | **true** |

#### 如果不用参数方案，要改源码怎么办？

`weight_kinematics_forward_drive` 对全向底盘不生效是因为 `EdgeKinematicsCarlike` 没有实现前进惩罚。要加的话需要：

1. Clone `teb_local_planner` 源码到 `~/catkin_ws/src/`
2. 修改 `edge_kinematics.h`，在全向运动学边中加入类似差速底盘的 `penaltyBoundFromBelow` 逻辑
3. `catkin_make` 重新编译

当前参数方案已能实现等效效果，无需改源码。

### 1.3 算力优化（释放 CPU）

| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `max_number_classes` | 4 | **2** | 同伦类数量减半 |
| `max_samples` | 500 | **300** | 最大采样数 |
| `no_outer_iterations` | 5 | **4** | 外迭代次数 |
| `enable_multithreading` | 未动 | true | 保持多线程开启 |

---

## 2. 🔴 代价地图参数（撞墙根因）

**文件**：`/home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/costmap_x4_params.yaml`
**备份**：`costmap_x4_params.yaml.bak`

| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `footprint` | `[[-0.11,-0.13],...]` (22×26cm) | `[[-0.18,-0.15],...]` (36×30cm) | 🔴 旧值比实际小 7cm/边 |
| `inflation_radius` | 0.15 | **0.35** | 与 TEB inflation_dist 对齐 |

> ⚠️ **这是撞墙的根因**：代价地图以 22cm 的机器人尺寸标障碍物，但真实机器人 36cm。转角处物理机器人超出 lethal 区域，持续碰撞。

---

## 3. 激光雷达过滤器

**文件**：`/home/abot/catkin_ws/src/abot_project/abot/param/lidar/x4_laserfilter.yaml`
**备份**：`x4_laserfilter.yaml.bak`

| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `max_x` | 0.15 | **0.20** | 自滤波盒覆盖机器人前端全部（18cm+2cm余量） |

**原理**：`LaserScanBoxFilter` 用于滤除打在机器人自身上的激光点。旧值 `max_x: 0.15` 比机器人前部（0.18m）少 3cm，导致前端激光反射未被过滤，产生假障碍物。

---

## 4. 🔴 TF 坐标变换延迟容限（撞墙的第二个根因）

### 4.1 日志诊断发现

分析今天 6 次导航会话的 ROS 日志，**100% 复现**同一个错误模式：

| 会话时间 | TF 外推错误 | 恢复行为（卡住） |
|---------|-----------|----------------|
| 13:32 | 4 | 4 |
| 13:56 | 4 | 2 |
| 14:34 | 27 | 2 |
| 14:53 | **58** | 4 |
| 15:00 | 15 | 2 |
| 15:25（最新） | 14+ | 2 |

每条错误内容：

```
TebLocalPlannerROS::transformGlobalPlan
Extrapolation Error: Lookup would require extrapolation into the future.
Requested time <T1> but the latest data is at time <T2>,
when looking up transform from frame [odom] to frame [map]
```

### 4.2 因果链

```
AMCL 定位更新（Jetson Nano 算力受限）
        ↓
map→odom 的 TF 变换发布滞后 100~400ms
        ↓
TEB::transformGlobalPlan() 无法将全局路径从 map 坐标系投影到 odom 坐标系
        ↓
TEB 用错误的/过期的全局路径做局部规划
        ↓
局部轨迹穿墙 → 物理碰撞
        ↓
move_base 检测到阻塞 → ClearCostmapRecovery 清空代价地图
        ↓
循环：清图→重新规划→TF滞后→再次撞墙
```

### 4.3 修改内容

**文件1**：`/home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/local_costmap_params.yaml`
**备份**：`local_costmap_params.yaml.bak`

| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `transform_tolerance` | 0.5 | **1.0** | TF 容忍度从 500ms 扩大到 1s |

**文件2**：`/home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/global_costmap_params.yaml`
**备份**：`global_costmap_params.yaml.bak`

| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `transform_tolerance` | 0.5 | **1.0** | TF 容忍度从 500ms 扩大到 1s |

### 4.4 原理

ROS 的 TF 系统允许多个节点在不同时间发布坐标变换。`transform_tolerance` 定义了"多旧的数据还能用"：

- 旧值 0.5s：只要 `map→odom` 变换比当前时刻旧 >500ms，就报 Extrapolation Error
- 新值 1.0s：容忍 1 秒内的旧数据
- Jetson Nano 上 AMCL 的定位更新间隔约 200-400ms，加上通信延迟，有时会突破 500ms
- 1 秒容忍度覆盖了绝大多数抖动场景

**代价**：使用更旧的 TF 数据意味着机器人可能基于 1 秒前的位置做规划——但因为代价地图本身更新频率只有 5Hz（200ms），且机器人最高速度 0.4m/s，1 秒最多走 40cm，在 3m×3m 的局部代价地图范围内仍然安全。

---

## 5. CPU 锁频服务

**文件**：`/etc/systemd/system/cpu-performance.service`
**类型**：新增文件

```ini
[Unit]
Description=Set CPU governor to performance
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'for cpu in /sys/devices/system/cpu/cpu[0-3]/cpufreq/scaling_governor; do echo performance > $cpu; done'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```

- 已 `systemctl enable`，每次开机自动执行
- 效果：CPU 锁定 1.48 GHz（performance 调度器）
- `nvpmodel` 已确认为 MAXN 模式（DEFAULT=0），无需修改

---

## 回档方法

### 回档单个文件
```bash
ssh abot@192.168.43.211
# 以 TEB 参数为例：
cp /home/abot/catkin_ws/src/abot_project/abot/param/navigation/teb/teb_omni_planner_params.yaml.bak \
   /home/abot/catkin_ws/src/abot_project/abot/param/navigation/teb/teb_omni_planner_params.yaml
```

### 回档全部修改
```bash
ssh abot@192.168.43.211

# TEB 参数
cp /home/abot/catkin_ws/src/abot_project/abot/param/navigation/teb/teb_omni_planner_params.yaml.bak \
   /home/abot/catkin_ws/src/abot_project/abot/param/navigation/teb/teb_omni_planner_params.yaml

# 代价地图
cp /home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/costmap_x4_params.yaml.bak \
   /home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/costmap_x4_params.yaml

# 激光过滤
cp /home/abot/catkin_ws/src/abot_project/abot/param/lidar/x4_laserfilter.yaml.bak \
   /home/abot/catkin_ws/src/abot_project/abot/param/lidar/x4_laserfilter.yaml

# TF 延迟容限
cp /home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/local_costmap_params.yaml.bak \
   /home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/local_costmap_params.yaml
cp /home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/global_costmap_params.yaml.bak \
   /home/abot/catkin_ws/src/abot_project/abot/param/navigation/common/global_costmap_params.yaml

# CPU 服务
sudo systemctl disable cpu-performance.service
sudo rm /etc/systemd/system/cpu-performance.service
```

### 生效方式
所有参数文件修改后，需要**重启导航节点**（重新 launch `navigate.launch`）才会生效。
CPU 锁频服务修改后需要 `sudo systemctl restart cpu-performance.service` 或重启。

---

## 备份文件清单

| 备份文件 | 对应原文件 |
|---------|-----------|
| `.../teb/teb_omni_planner_params.yaml.bak` | teb_omni_planner_params.yaml |
| `.../common/costmap_x4_params.yaml.bak` | costmap_x4_params.yaml |
| `.../lidar/x4_laserfilter.yaml.bak` | x4_laserfilter.yaml |
| `.../common/local_costmap_params.yaml.bak` | local_costmap_params.yaml |
| `.../common/global_costmap_params.yaml.bak` | global_costmap_params.yaml |

> 完整路径前缀：`/home/abot/catkin_ws/src/abot_project/abot/param/navigation/`
